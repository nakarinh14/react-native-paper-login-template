import { DefaultTheme } from 'react-native-paper';

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#FFFD00',
    secondary: '#FFFFFF',
    tertiary: '#00FCF6',
    error: '#f13a59',
  },
};
